import Phaser from 'phaser';

// Step 5+: Victory / defeat screen with stats breakdown
export default class ResultScene extends Phaser.Scene {
  constructor() { super({ key: 'ResultScene' }); }

  create(): void {
    // TODO (Step 5): Stats display, XP reward, return to town/retry
  }
}

import Phaser from 'phaser';

// Step 3+: Tilemap-based dungeon with enemy patrol + battle triggers
export default class DungeonScene extends Phaser.Scene {
  constructor() { super({ key: 'DungeonScene' }); }

  create(): void {
    // TODO (Step 3): Tilemap loader, collision, enemy spawns
  }
}

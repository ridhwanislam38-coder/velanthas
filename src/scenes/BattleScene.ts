import Phaser from 'phaser';

// Step 5: Core battle loop — questions, timer, damage, LUMINA BREAK
export default class BattleScene extends Phaser.Scene {
  constructor() { super({ key: 'BattleScene' }); }

  create(): void {
    // TODO (Step 5): Full battle UI + DamageSystem integration
  }
}
